import {
  StyleSheet,
} from 'react-native';
import colors from '../../res/colors'

export default StyleSheet.create({
  footer: {
    height: '10%',
    width: '100%',
    backgroundColor: colors.homeRed,
    position: 'absolute',
    bottom: 0,
  }
});
